'use strict';

for( let num1, num2; ; ){
    
    num1 = +prompt("정수1 : ")
    num2 = +prompt("정수2 : ")
    if(num1 > num2){
        alert(`큰수는 ${t}입니다.`)
    } else {
        alert(`두 수는 같음`)
        if (t > num2) {
        
        }
    }
    if(confirm("창을 닫을 까요?")) {
        break;
    }
}